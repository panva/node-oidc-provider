/* eslint-disable camelcase */

const form_post = require('./form_post');
const fragment = require('./fragment');
const query = require('./query');
const webMessage = require('./web_message');

module.exports = {
  form_post,
  fragment,
  query,
  webMessage,
};
